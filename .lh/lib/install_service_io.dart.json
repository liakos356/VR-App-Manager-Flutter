{
    "sourceFile": "lib/install_service_io.dart",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 1,
            "patches": [
                {
                    "date": 1776248994093,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1776374179753,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,5 +1,6 @@\n import 'dart:io';\n+\n import 'package:dart_smb2/dart_smb2.dart';\n import 'package:flutter/services.dart';\n import 'package:permission_handler/permission_handler.dart';\n \n@@ -16,21 +17,22 @@\n     // Request necessary permissions for Android 10+ devices (like Pico 4)\n     if (Platform.isAndroid) {\n       onProgress('Requesting permissions...');\n       await Permission.requestInstallPackages.request();\n-      \n+\n       if (!await Permission.manageExternalStorage.isGranted) {\n         onProgress('Please grant All Files Access in Settings if prompted...');\n         await Permission.manageExternalStorage.request();\n       }\n-      \n+\n       if (!await Permission.storage.isGranted) {\n         await Permission.storage.request();\n       }\n     }\n \n     Smb2Pool? pool;\n     try {\n+      print('[InstallService] Connecting to SMB for $appId...');\n       onProgress('Connecting to SMB...');\n       pool = await Smb2Pool.connect(\n         host: '100.95.32.89',\n         share: 'ssd_internal',\n@@ -40,179 +42,300 @@\n       );\n \n       final dlPath = '/sdcard/Download/$appId.apk';\n       final apkFile = File(dlPath);\n+      print('[InstallService] DL path set to ${apkFile.path}');\n \n-      onProgress('Checking SMB for APK...');\n+      int totalSizeToDownload = 0;\n+      int totalDownloaded = 0;\n \n-      bool apkFound = false;\n-\n-      Future<bool> downloadWithProgress(String remotePath, File localFile) async {\n+      Future<int> getRemoteSize(String remotePath) async {\n         try {\n+          print('[InstallService] Checking remote size for: $remotePath');\n           final exists = await pool!.exists(remotePath);\n-          if (!exists) return false;\n-          \n+          if (!exists) {\n+            print('[InstallService] Remote file does not exist: $remotePath');\n+            return 0;\n+          }\n           final size = await pool.fileSize(remotePath);\n-          final stream = pool.streamFile(remotePath, chunkSize: 1024 * 1024 * 2);\n-          \n-          int downloaded = 0;\n+          print('[InstallService] Remote size for $remotePath is $size bytes');\n+          return size;\n+        } catch (e) {\n+          print('[InstallService] Size check failed for $remotePath: $e');\n+          onProgress('Size check failed for $remotePath: $e');\n+        }\n+        return 0;\n+      }\n+\n+      Future<bool> downloadFile(\n+        String remotePath,\n+        File localFile,\n+        int size,\n+      ) async {\n+        try {\n+          print(\n+            '[InstallService] Beginning download: $remotePath -> ${localFile.path} ($size bytes)',\n+          );\n+          onProgress(\n+            'Downloading ${localFile.path.split('/').last} ($size bytes)...',\n+          );\n+          if (size == 0) {\n+            print('[InstallService] Error: Remote file size is 0 bytes.');\n+            onProgress('Error: Remote file size is 0 bytes.');\n+            return false;\n+          }\n+\n+          final stream = pool!.streamFile(\n+            remotePath,\n+            chunkSize: 1024 * 1024 * 2,\n+          );\n+\n           final sink = localFile.openWrite();\n           try {\n             await for (var chunk in stream) {\n               sink.add(chunk);\n-              downloaded += chunk.length;\n-              if (size > 0 && onDownloadProgress != null) {\n-                onDownloadProgress(downloaded / size);\n+              totalDownloaded += (chunk as List<int>).length;\n+              if (totalSizeToDownload > 0 && onDownloadProgress != null) {\n+                onDownloadProgress(totalDownloaded / totalSizeToDownload);\n               }\n             }\n           } finally {\n             await sink.flush();\n             await sink.close();\n           }\n+\n+          final finalSize = await localFile.length();\n+          print(\n+            '[InstallService] Download finished. Final local size: $finalSize bytes',\n+          );\n+          onProgress('Downloaded $finalSize bytes.');\n+\n+          if (finalSize < size) {\n+            print(\n+              '[InstallService] Download incomplete! Expected $size, got $finalSize',\n+            );\n+            onProgress('Download incomplete for ${localFile.path}');\n+            return false;\n+          }\n+\n           return true;\n         } catch (e) {\n+          print('[InstallService] Download stream failed: $e');\n           onProgress('Download stream failed: $e');\n           return false;\n         }\n       }\n \n-      // Extract the relative path without the mount prefix since SMB starts at share root\n-      // E.g., /mnt/sda/downloads/... -> downloads/...\n+      // Phase 1: Determine files to download and their sizes\n+      String apkRemotePathToDownload = '';\n+      int apkSize = 0;\n+\n       String relativeApkPath = apkPath;\n       if (relativeApkPath.startsWith('/mnt/sda/')) {\n         relativeApkPath = relativeApkPath.substring('/mnt/sda/'.length);\n       } else if (relativeApkPath.startsWith('/')) {\n         relativeApkPath = relativeApkPath.substring(1);\n       }\n-\n-      // Ensure we use backslashes for SMB paths just in case\n+      if (relativeApkPath.startsWith('ssd_internal/')) {\n+        relativeApkPath = relativeApkPath.substring('ssd_internal/'.length);\n+      }\n       relativeApkPath = relativeApkPath.replaceAll('/', '\\\\');\n+      print('[InstallService] Normalized relative APK path: $relativeApkPath');\n \n-      onProgress('Trying direct path: $relativeApkPath');\n-\n-      // Try direct apk first using path from DB\n-      try {\n-        onProgress('Downloading APK: $relativeApkPath...');\n-        apkFound = await downloadWithProgress(relativeApkPath, apkFile);\n-      } catch (e) {\n-        onProgress('Direct path failed: $e');\n+      onProgress('Locating APK...');\n+      if (relativeApkPath.isNotEmpty) {\n+        try {\n+          apkSize = await getRemoteSize(relativeApkPath);\n+          if (apkSize > 0) apkRemotePathToDownload = relativeApkPath;\n+        } catch (e) {\n+          print('[InstallService] Error getting size for relativeApkPath: $e');\n+        }\n       }\n \n-      if (!apkFound) {\n-        onProgress(\n-          'Checking alternate path downloads/pico4/apps/$appId.apk...',\n-        );\n+      if (apkSize == 0 || apkRemotePathToDownload.isEmpty) {\n+        final altApkPath = 'downloads\\\\pico4\\\\apps\\\\$appId.apk';\n         try {\n-          onProgress('Downloading APK...');\n-          apkFound = await downloadWithProgress('downloads\\\\pico4\\\\apps\\\\$appId.apk', apkFile);\n+          apkSize = await getRemoteSize(altApkPath);\n+          if (apkSize > 0) apkRemotePathToDownload = altApkPath;\n         } catch (e) {\n-          // Ignore if alternate path not found\n+          print('[InstallService] Error getting size for altApkPath: $e');\n         }\n       }\n \n-      if (!apkFound) {\n-        // Try looking in a folder: downloads/pico4/apps/[appId]/\n-        onProgress('Checking folder downloads/pico4/apps/$appId...');\n+      if (apkSize == 0 || apkRemotePathToDownload.isEmpty) {\n         final folder = 'downloads\\\\pico4\\\\apps\\\\$appId';\n-\n-        List<dynamic> files = [];\n+        print('[InstallService] Checking inside folder: $folder');\n         try {\n-          files = await pool.listDirectory(folder);\n+          List<dynamic> files = await pool.listDirectory(folder);\n+          print('[InstallService] Found ${files.length} items in $folder');\n+          for (var f in files) {\n+            String name = f.name.toString();\n+            if (name.endsWith('.apk')) {\n+              final checkPath = '$folder\\\\$name';\n+              apkSize = await getRemoteSize(checkPath);\n+              if (apkSize > 0) {\n+                apkRemotePathToDownload = checkPath;\n+                break;\n+              }\n+            }\n+          }\n         } catch (e) {\n-          // Ignore if folder not found\n+          print(\n+            '[InstallService] Error listing directory for APK fallback: $e',\n+          );\n         }\n+      }\n \n-        for (var f in files) {\n-          if (f.name.endsWith('.apk')) {\n-            onProgress('Downloading APK: ${f.name}...');\n-            apkFound = await downloadWithProgress('$folder\\\\${f.name}', apkFile);\n-            if (apkFound) break;\n-          }\n-        }\n-\n-        if (!apkFound) {\n-          throw Exception('No APK found for $appId on SMB');\n-        }\n+      if (apkSize == 0 || apkRemotePathToDownload.isEmpty) {\n+        print('[InstallService] Critical: No APK found on SMB for $appId.');\n+        throw Exception('No APK found for $appId on SMB');\n       }\n \n-      // Also look for OBB files\n-      onProgress('Checking OBB...');\n-      \n-      // Try reading from the specified obb directory in DB if available\n+      print(\n+        '[InstallService] APK located: $apkRemotePathToDownload ($apkSize bytes)',\n+      );\n+\n+      onProgress('Locating OBBs...');\n       String relativeObbPath = obbDir;\n       if (relativeObbPath.startsWith('/mnt/sda/')) {\n         relativeObbPath = relativeObbPath.substring('/mnt/sda/'.length);\n       } else if (relativeObbPath.startsWith('/')) {\n         relativeObbPath = relativeObbPath.substring(1);\n       }\n-\n+      if (relativeObbPath.startsWith('ssd_internal/')) {\n+        relativeObbPath = relativeObbPath.substring('ssd_internal/'.length);\n+      }\n       relativeObbPath = relativeObbPath.replaceAll('/', '\\\\');\n+      print('[InstallService] Normalized relative OBB path: $relativeObbPath');\n \n       String obbFolderName = appId;\n       if (relativeObbPath.isNotEmpty) {\n         final segments = relativeObbPath.split('\\\\');\n         if (segments.isNotEmpty && segments.last.isNotEmpty) {\n           obbFolderName = segments.last;\n         }\n       }\n+      print('[InstallService] Expected OBB folder name: $obbFolderName');\n+\n       final localObbDir = Directory('/sdcard/Android/obb/$obbFolderName');\n+      print(\n+        '[InstallService] Local OBB Directory will be: ${localObbDir.path}',\n+      );\n+      List<Map<String, dynamic>> obbFilesToDownload = [];\n \n-      List<dynamic> obbFiles = [];\n+      final folderToUseObb = relativeObbPath.isNotEmpty\n+          ? relativeObbPath\n+          : 'downloads\\\\pico4\\\\apps\\\\$appId';\n+\n+      print('[InstallService] SMB folder to check for OBBs: $folderToUseObb');\n+\n       try {\n-        if (relativeObbPath.isNotEmpty) {\n-          onProgress('Checking OBB in $relativeObbPath...');\n-          obbFiles = await pool.listDirectory(relativeObbPath);\n-        } else {\n-          onProgress('Checking OBB in downloads\\\\pico4\\\\apps\\\\$appId...');\n-          obbFiles = await pool.listDirectory('downloads\\\\pico4\\\\apps\\\\$appId');\n+        final obbList = await pool.listDirectory(folderToUseObb);\n+        print(\n+          '[InstallService] Found ${obbList.length} items in $folderToUseObb',\n+        );\n+        for (var f in obbList) {\n+          String name = f.name.toString();\n+          if (name.endsWith('.obb')) {\n+            final obbRemotePath = '$folderToUseObb\\\\$name';\n+            final obbLocalFile = File('${localObbDir.path}/$name');\n+            final size = await getRemoteSize(obbRemotePath);\n+            print('[InstallService] Found OBB file $name with size $size');\n+            if (size > 0) {\n+              obbFilesToDownload.add({\n+                'remotePath': obbRemotePath,\n+                'localFile': obbLocalFile,\n+                'size': size,\n+              });\n+            }\n+          }\n         }\n       } catch (e) {\n-        onProgress('OBB directory not found: $e');\n+        print(\n+          '[InstallService] Error retrieving OBBs from $folderToUseObb: $e',\n+        );\n       }\n \n-      final folderToUse = relativeObbPath.isNotEmpty\n-          ? relativeObbPath\n-          : 'downloads\\\\pico4\\\\apps\\\\$appId';\n+      // Calculate total sizes\n+      for (var obb in obbFilesToDownload) {\n+        totalSizeToDownload += obb['size'] as int;\n+      }\n+      totalSizeToDownload += apkSize;\n \n-      for (var f in obbFiles) {\n-        if (f.name.endsWith('.obb')) {\n-          onProgress('Downloading OBB ${f.name}...');\n+      print(\n+        '[InstallService] TOTAL sizes to download calculated: $totalSizeToDownload (APK: $apkSize, OBB count: ${obbFilesToDownload.length})',\n+      );\n \n-          if (!await localObbDir.exists()) {\n-            try {\n-              await localObbDir.create(recursive: true);\n-            } catch (e) {\n-              throw Exception(\n-                'Permission denied for ${localObbDir.path}. '\n-                'Go to Pico Settings -> Apps -> VR App Manager -> Permissions and enable \"All Files Access\".\\n\\n'\n-                'Details: $e',\n-              );\n-            }\n-          }\n-          final localObbFile = File('${localObbDir.path}/${f.name}');\n+      // Phase 2: Execute downloads. Download OBB first, then APK.\n+      if (obbFilesToDownload.isNotEmpty) {\n+        print('[InstallService] Starting Phase 2: Downloading OBBs...');\n+        if (!await localObbDir.exists()) {\n           try {\n-            final success = await downloadWithProgress('$folderToUse\\\\${f.name}', localObbFile);\n-            if (!success) {\n-               throw Exception('OBB download stream failed for ${f.name}');\n-            }\n+            print('[InstallService] Creating local OBB directory recursive');\n+            await localObbDir.create(recursive: true);\n           } catch (e) {\n-            throw Exception('Failed to write OBB file. Please ensure storage permissions are granted. Error: $e');\n+            print('[InstallService] Failed to create local OBB directory: $e');\n+            throw Exception(\n+              'Permission denied for ${localObbDir.path}. '\n+              'Go to Pico Settings -> Apps -> VR App Manager -> Permissions and enable \"All Files Access\".\\n\\n'\n+              'Details: $e',\n+            );\n           }\n         }\n+\n+        for (var obb in obbFilesToDownload) {\n+          print(\n+            '[InstallService] Initiating download of OBB: ${obb['remotePath']} -> ${obb['localFile']}',\n+          );\n+          final success = await downloadFile(\n+            obb['remotePath'] as String,\n+            obb['localFile'] as File,\n+            obb['size'] as int,\n+          );\n+          if (!success) {\n+            print('[InstallService] OBB download step returned false!');\n+            throw Exception('Failed to download OBB: ${obb['remotePath']}');\n+          }\n+        }\n       }\n \n+      print(\n+        '[InstallService] Starting Phase 2 (or continuing Phase 2): Downloading APK... $apkRemotePathToDownload',\n+      );\n+      onProgress('Downloading APK...');\n+      final apkSuccess = await downloadFile(\n+        apkRemotePathToDownload,\n+        apkFile,\n+        apkSize,\n+      );\n+      if (!apkSuccess) {\n+        print('[InstallService] APK download failed!');\n+        throw Exception('Failed to download APK');\n+      }\n+\n+      print(\n+        '[InstallService] Files downloaded successfully. Triggering native APK Installation via intent.',\n+      );\n       onProgress('Triggering Native Install...');\n       final result = await platform.invokeMethod('installApk', {\n         'apkPath': apkFile.path,\n       });\n       if (result == true) {\n+        print('[InstallService] Native install intent successful.');\n         onProgress('Installation Started!');\n       } else {\n-        throw Exception('Installation Intent Failed');\n+        print('[InstallService] Native install intent returned FALSE.');\n+        throw Exception(\n+          'Installation Intent Failed: The APK might be corrupt, incompletely downloaded, or its CPU architecture (e.g. arm64) is not supported by this device (e.g. x86_64 Emulator).',\n+        );\n       }\n     } catch (e) {\n+      print(\n+        '[InstallService] Unhandled Exception during overall install sequence: $e',\n+      );\n       throw Exception('Install Error: $e');\n     } finally {\n+      print('[InstallService] Disconnecting from SMB pool...');\n       await pool?.disconnect();\n     }\n   }\n }\n"
                }
            ],
            "date": 1776248994093,
            "name": "Commit-0",
            "content": "import 'dart:io';\nimport 'package:dart_smb2/dart_smb2.dart';\nimport 'package:flutter/services.dart';\nimport 'package:permission_handler/permission_handler.dart';\n\nclass InstallService {\n  static const platform = MethodChannel('com.vr.appmanager/install');\n\n  static Future<void> installAppLocally({\n    required String appId,\n    required String apkPath,\n    required String obbDir,\n    required Function(String) onProgress,\n    Function(double)? onDownloadProgress,\n  }) async {\n    // Request necessary permissions for Android 10+ devices (like Pico 4)\n    if (Platform.isAndroid) {\n      onProgress('Requesting permissions...');\n      await Permission.requestInstallPackages.request();\n      \n      if (!await Permission.manageExternalStorage.isGranted) {\n        onProgress('Please grant All Files Access in Settings if prompted...');\n        await Permission.manageExternalStorage.request();\n      }\n      \n      if (!await Permission.storage.isGranted) {\n        await Permission.storage.request();\n      }\n    }\n\n    Smb2Pool? pool;\n    try {\n      onProgress('Connecting to SMB...');\n      pool = await Smb2Pool.connect(\n        host: '100.95.32.89',\n        share: 'ssd_internal',\n        user: 'liakos',\n        password: 'stella.elias.240922',\n        workers: 1,\n      );\n\n      final dlPath = '/sdcard/Download/$appId.apk';\n      final apkFile = File(dlPath);\n\n      onProgress('Checking SMB for APK...');\n\n      bool apkFound = false;\n\n      Future<bool> downloadWithProgress(String remotePath, File localFile) async {\n        try {\n          final exists = await pool!.exists(remotePath);\n          if (!exists) return false;\n          \n          final size = await pool.fileSize(remotePath);\n          final stream = pool.streamFile(remotePath, chunkSize: 1024 * 1024 * 2);\n          \n          int downloaded = 0;\n          final sink = localFile.openWrite();\n          try {\n            await for (var chunk in stream) {\n              sink.add(chunk);\n              downloaded += chunk.length;\n              if (size > 0 && onDownloadProgress != null) {\n                onDownloadProgress(downloaded / size);\n              }\n            }\n          } finally {\n            await sink.flush();\n            await sink.close();\n          }\n          return true;\n        } catch (e) {\n          onProgress('Download stream failed: $e');\n          return false;\n        }\n      }\n\n      // Extract the relative path without the mount prefix since SMB starts at share root\n      // E.g., /mnt/sda/downloads/... -> downloads/...\n      String relativeApkPath = apkPath;\n      if (relativeApkPath.startsWith('/mnt/sda/')) {\n        relativeApkPath = relativeApkPath.substring('/mnt/sda/'.length);\n      } else if (relativeApkPath.startsWith('/')) {\n        relativeApkPath = relativeApkPath.substring(1);\n      }\n\n      // Ensure we use backslashes for SMB paths just in case\n      relativeApkPath = relativeApkPath.replaceAll('/', '\\\\');\n\n      onProgress('Trying direct path: $relativeApkPath');\n\n      // Try direct apk first using path from DB\n      try {\n        onProgress('Downloading APK: $relativeApkPath...');\n        apkFound = await downloadWithProgress(relativeApkPath, apkFile);\n      } catch (e) {\n        onProgress('Direct path failed: $e');\n      }\n\n      if (!apkFound) {\n        onProgress(\n          'Checking alternate path downloads/pico4/apps/$appId.apk...',\n        );\n        try {\n          onProgress('Downloading APK...');\n          apkFound = await downloadWithProgress('downloads\\\\pico4\\\\apps\\\\$appId.apk', apkFile);\n        } catch (e) {\n          // Ignore if alternate path not found\n        }\n      }\n\n      if (!apkFound) {\n        // Try looking in a folder: downloads/pico4/apps/[appId]/\n        onProgress('Checking folder downloads/pico4/apps/$appId...');\n        final folder = 'downloads\\\\pico4\\\\apps\\\\$appId';\n\n        List<dynamic> files = [];\n        try {\n          files = await pool.listDirectory(folder);\n        } catch (e) {\n          // Ignore if folder not found\n        }\n\n        for (var f in files) {\n          if (f.name.endsWith('.apk')) {\n            onProgress('Downloading APK: ${f.name}...');\n            apkFound = await downloadWithProgress('$folder\\\\${f.name}', apkFile);\n            if (apkFound) break;\n          }\n        }\n\n        if (!apkFound) {\n          throw Exception('No APK found for $appId on SMB');\n        }\n      }\n\n      // Also look for OBB files\n      onProgress('Checking OBB...');\n      \n      // Try reading from the specified obb directory in DB if available\n      String relativeObbPath = obbDir;\n      if (relativeObbPath.startsWith('/mnt/sda/')) {\n        relativeObbPath = relativeObbPath.substring('/mnt/sda/'.length);\n      } else if (relativeObbPath.startsWith('/')) {\n        relativeObbPath = relativeObbPath.substring(1);\n      }\n\n      relativeObbPath = relativeObbPath.replaceAll('/', '\\\\');\n\n      String obbFolderName = appId;\n      if (relativeObbPath.isNotEmpty) {\n        final segments = relativeObbPath.split('\\\\');\n        if (segments.isNotEmpty && segments.last.isNotEmpty) {\n          obbFolderName = segments.last;\n        }\n      }\n      final localObbDir = Directory('/sdcard/Android/obb/$obbFolderName');\n\n      List<dynamic> obbFiles = [];\n      try {\n        if (relativeObbPath.isNotEmpty) {\n          onProgress('Checking OBB in $relativeObbPath...');\n          obbFiles = await pool.listDirectory(relativeObbPath);\n        } else {\n          onProgress('Checking OBB in downloads\\\\pico4\\\\apps\\\\$appId...');\n          obbFiles = await pool.listDirectory('downloads\\\\pico4\\\\apps\\\\$appId');\n        }\n      } catch (e) {\n        onProgress('OBB directory not found: $e');\n      }\n\n      final folderToUse = relativeObbPath.isNotEmpty\n          ? relativeObbPath\n          : 'downloads\\\\pico4\\\\apps\\\\$appId';\n\n      for (var f in obbFiles) {\n        if (f.name.endsWith('.obb')) {\n          onProgress('Downloading OBB ${f.name}...');\n\n          if (!await localObbDir.exists()) {\n            try {\n              await localObbDir.create(recursive: true);\n            } catch (e) {\n              throw Exception(\n                'Permission denied for ${localObbDir.path}. '\n                'Go to Pico Settings -> Apps -> VR App Manager -> Permissions and enable \"All Files Access\".\\n\\n'\n                'Details: $e',\n              );\n            }\n          }\n          final localObbFile = File('${localObbDir.path}/${f.name}');\n          try {\n            final success = await downloadWithProgress('$folderToUse\\\\${f.name}', localObbFile);\n            if (!success) {\n               throw Exception('OBB download stream failed for ${f.name}');\n            }\n          } catch (e) {\n            throw Exception('Failed to write OBB file. Please ensure storage permissions are granted. Error: $e');\n          }\n        }\n      }\n\n      onProgress('Triggering Native Install...');\n      final result = await platform.invokeMethod('installApk', {\n        'apkPath': apkFile.path,\n      });\n      if (result == true) {\n        onProgress('Installation Started!');\n      } else {\n        throw Exception('Installation Intent Failed');\n      }\n    } catch (e) {\n      throw Exception('Install Error: $e');\n    } finally {\n      await pool?.disconnect();\n    }\n  }\n}\n"
        }
    ]
}